// AWS Amplify configuration placeholder
// Replace with real values when you set up Cognito, Lex, etc.
const awsconfig = {
  // Leave empty for now – just to resolve the import
};
export default awsconfig;
